# 27. 3D effects for the panel

**Worktree:** `/Users/apple/AnimatedPixelClock-fx3d` - **branch:** `feat/fx3d` -
**flag:** `-DFX3D_ENABLED`. Started 2026-09-18. See [00](00-how-we-work.md).

## What it is for

The owner's words, not a paraphrase.

- **2026-09-18 14:34:** «Это сессия дл проектирования 3Д Эффетков для лед экрана»
- **About 14:40, four questions, every option taken:**

  | Question | Answer |
  |---|---|
  | Where on the panel | separate pages, a 3D clock, ambient, to music |
  | What kind of 3D | solid lit objects, vector/wireframe, fly-throughs and spaces (voxel landscape, tunnel, grid), raymarching (SDF) |
  | Data or beauty | beauty only, a globe with the real day and night, other data in 3D |
  | How to choose | previews first, then the choice |

- **14:40:** «Еще забери проработку этого вопроса от других коллег давших нам идею,
  забери файл Waveshare_2Panel_Anaglyph3D_Claude_Code_Handoff.md из даунлоадс и
  сохрани себе и изучи» - kept byte for byte as
  [drafts/27-anaglyph-handoff-2026-09-18.md](drafts/27-anaglyph-handoff-2026-09-18.md).
  That brief defines "3D" as **anaglyph stereo**: two perspective projections of
  one scene in the red and blue (or green+blue) channels of one frame, viewed
  through the owner's PYRUVAE red/blue glasses.
- **15:22:** «нужно это функцию 3Д делать как функцию, чтобы любой экран можно
  было переключить в 3Д, не переделывая, а просто выбирая режим отображения.»
- **15:23:** «закладывать такую архитектуру и идти к ней пошагово, конечно, нужно
  сначал вообще понять что плучается, но заклдывать все возмодности нужно»
- **15:24:** «и отдельные эффектные сцены нам тоже нужны, на них мы трененруемся и
  отрабатываем мВП»
- **15:44**, asked which scenes and looks he likes: «все, но нужно смотреть с очками»

## The architecture: two layers

**Scenes** are 3D content: pages of their own, where the depth is real
geometry. They are the training ground and the MVP (the owner, 15:24).
**Looks** are 3D as a display mode: any page draws its ordinary 2D frame,
nothing about it changes, and the chosen look shows that frame in 3D (the
owner, 15:22). Both run on one model, `src/fx3d/`, and both work in mono and
with the glasses.

```
page's 2D frame --capture--> look  --+
                                     +--> mono, or left+right --> compose --> encode (CIE) --> blit
scene (real 3D) ---------------------+
```

1. *Scene or look*: 3D coordinates and a time. Both eyes see the same moment.
2. *Projection*: mono, or off-axis stereo - parallel cameras at x = ±b/2 and a
   zero-parallax plane at Z0 (the brief §4). No toe-in.
3. *Raster*: into **linear-light** buffers - points at sub-pixel positions, Wu
   lines, integer-edge triangles with a depth test, per-pixel rays for the
   fly-throughs.
4. *Compose*: mono colour, or anaglyph (red-blue first, red-cyan, red-green),
   with per-eye gains and an eye swap. Additive: one eye never erases the other.
5. *Encode once*: linear light → the CIE lightness code the library expects,
   through the inverse of the library's own table. No double gamma.
6. *Blit*: 128×64 from PSRAM with `drawPixelRGB888`, like the Lua effects.

**Depth budget.** A point at depth Z has disparity d = f·b·(1/Z − 1/Z0)
pixels. A scene declares its depth range and b is chosen so |d| stays within
`depthPx`, which starts at 2 (the brief §4: 1-2 px, an experiment setting,
not a norm). Vertices are never clamped one by one. The looks that move pixels
spend `depthPx` directly.

### The scenes (14)

Host cost is microseconds per frame on the Mac - for comparing scenes with
each other only; **the panel's cost is unmeasured** until the bench runs.
The scene's object lives in PSRAM on the panel.

| Scene | What | Place | Mac µs mono / anaglyph | Object |
|---|---|---|---|---|
| `calib` | the brief §7: pure R, G, B with scales; a bar per eye (crosstalk, swap); the panel's plane and seam; squares at +d, 0, −d | service page | 21 / 9 | 0 KB |
| `cube` | a wire cube turning about the panel's plane | page, ambient | 6 / 9 | 0 KB |
| `layers` | ring in front, square on the panel, triangle behind | page | 8 / 10 | 0 KB |
| `stars` | flight through a seeded field; faded near the edges in front of the panel | ambient | 10 / 15 | 1.7 KB |
| `helix` | a double helix turning on its own axis | ambient | 11 / 16 | 0 KB |
| `rings` | rings coming down a winding tube, all behind the panel | ambient | 14 / 20 | 0 KB |
| `dial` | dial on the panel, numerals in front, hands nearer, HH MM behind | clock style | 9 / 13 | 0 KB |
| `torus` | Gouraud-lit torus, white highlight | page, ambient | 28 / 50 | 7.4 KB |
| `vclock` | HH:MM in voxels, a changed digit flips | clock style | 19 / 32 | 0.1 KB |
| `voxel` | Comanche-style landscape, 256×256 map from the seed | ambient | 66 / 118 | 320 KB |
| `tunnel` | neon tube; geometry once per eye, the frame slides the pattern | ambient | 57 / 105 | 96 KB |
| `blobs` | four metaballs, sphere-traced | ambient | 580 / 1137 | 0.1 KB |
| `globe` | the Earth lit as now: the world clock's mask and sun model, its cities | page by the world clock | 80 / 154 | 0.7 KB |
| `terrain` | the visualizer's spectrum as hills; a beat lights the front ridge | visualizer | 62 / 113 | 2.4 KB |

`blobs` is ten times the others on the Mac: the bench decides whether it runs
at full resolution on the panel.

### The looks (8 and flat)

| Look | Family | How |
|---|---|---|
| `flat` | - | the page as it is |
| `pop` | glasses | brighter is nearer; black stays on the panel's plane |
| `layers` | glasses | warm colours in front, white in the middle, cold behind (a design choice, not a measured rule) |
| `float` | glasses | the whole picture one plane in front - the brief's "only a flat layer", kept as the baseline |
| `dome` | glasses | the middle bulges towards the viewer |
| `wiggle` | no glasses | the `pop` depth seen from an eye swaying sideways about twice a second |
| `card` | no glasses | the picture on a card swaying in space |
| `relief` | no glasses | lit pixels raised as blocks, their sides in shade, oblique projection |
| `drum` | no glasses | wrapped round a drum turning to and fro |

The glasses looks move each pixel sideways by its own disparity, per eye,
row by row: same-depth pixels tile and add, depth bands are laid far to near,
uncovered pixels stay black. `card`, `relief` and `drum` are real geometry,
so they work with the glasses too. Tried on six real pages on the host
(`tools/fx3d/looks.py`): the snake, tetris and world clocks, Minecraft, the
snooker clock and the weather screen. Big digits on black are where the
glasses looks read best; on a full lit picture `pop` becomes `float`.

### How a look reaches any page on the panel (next step)

The DMA frame buffer is private in ESP32-HUB75-MatrixPanel-DMA 3.0.14, so the
frame is captured where it is drawn, not read back. Under the flag the global
`display` becomes a subclass (`src/fx3d/`) that overrides the library's five
virtual drawing calls (`drawPixel`, `fillScreen`, `fillRect`,
`drawFastVLine`, `drawFastHLine`) and hides `drawPixelRGB888`,
`fillScreenRGB888`, `clearScreen`, `clearDisplay` and `display()`. Every
page reaches the panel through those: 111 `print`, 199 `fillRect`, 190
`drawPixel`… across `src/`, and no code holds a base-class pointer to
`display`. While a look is on, the calls fill a PSRAM copy of the frame; at
`display()` the look turns it into the 3D frame, which then goes to the
panel. With the look off, the calls pass straight through. With the flag off,
nothing changes.

## Constraints it has to live inside

| Constraint | Figure | Source |
|---|---|---|
| Internal heap | about 33 KB free; a portal page load takes another ~20 KB | doc 22 §12.1 (32,952 B idle), §12.2 |
| `loop()`'s stack | 8 KB for everything `loop()` calls: no big arrays on the stack | `src/lua/README.md` (`CONFIG_ARDUINO_LOOP_STACK_SIZE`) |
| Render tick | 20 Hz animated clocks, 30 Hz ambient, 60 Hz while an animation runs | `getOptimalRefreshRate()` in `main.cpp` |
| Panel scan | 84 Hz; a flip takes effect at the end of a scan | [14](14-lua.md), `MatrixDisplay::waitForScanCompletion()` |
| Pixel write | per pixel: three CIE table lookups, then a read-modify-write of one word in each of 8 bit planes. **Cost of a full 8192-pixel blit: not measured** | library `updateMatrixDMABuffer()` |
| CPU | two Xtensa LX7 at 240 MHz, single-precision FPU; `double` is emulated in software | ESP32-S3 datasheet; ESP-IDF *Speed Optimization* |
| Float costs | in a call loop on an S3: `cosf` 121 cycles, float divide 69, `double` `cos` 1619 | Espressif blog "Floating-Point Units on Espressif SoCs", 2025-10 |
| A task that uses float | IDF FreeRTOS pins it to the core it runs on | ESP-IDF FreeRTOS (IDF), *Floating Point Usage* |
| Large buffers | PSRAM: 16 MB octal. Precedent: the Lua effects keep 4 × 24,576 B canvases there | `platformio.ini`, `src/lua/README.md` |
| A task stack | internal RAM only on arduino-esp32 2.0.17 | `src/lua/README.md` |
| C++ standard | the firmware builds as gnu++11, so the model must too; the host test builds c++11 and c++17 | HANDOFF 2026-09-16 |
| Flash | app slot 4.5 MB, image ~2.24 MB | `platformio.ini`, HANDOFF 2026-09-15 |
| Panel pitch | 2 mm (SKU 33838); the "P2.5" in `matrix_display.h` is upstream's hardware and stays | [01](01-panel.md), the integration session |
| Brightness | not the maximum | [04](04-best-practices.md) |

## The colleagues' brief against what this project already knows

| The brief says | Here | Source |
|---|---|---|
| a separate project, NickoScope's firmware untouched | the panel's own fork, a module behind `FX3D_ENABLED`, off by default; NickoScope32 is not touched | [00](00-how-we-work.md) |
| two Waveshare 64×64, 2 mm, SKU unknown | SKU 33838, `RGB-Matrix-P2-64x64-B`, 2 mm, GOB | [01](01-panel.md) |
| controller unknown; Pi 5 or ESP32-S3 | Waveshare ESP32-S3-RGB-Matrix, WROOM-2 N32R16V, verified on the panel | [02](02-controller.md), [12](12-bringup.md) |
| HUB75/HUB75E a hypothesis; chaining, mapping unknown | HUB75E, FM6126A, `clkphase=false`, two panels as one 128×64 canvas, phase 3 passed | `src/display/matrix_display.h`, [12](12-bringup.md) |
| library version and licence to be pinned | ESP32-HUB75-MatrixPanel-DMA **3.0.14**, pinned | `platformio.ini` |
| PSRAM does not guarantee DMA buffers | tried: stripes and broken TLS; the buffers stay internal | [03](03-firmware.md) |
| find what gamma the backend already applies | CIE 1931 on every `drawPixelRGB888` (8-bit, `NO_CIE1931` not defined); our copy of the table is checked byte for byte against the library's file | library `cie_luts.h`, `tools/fx3d/check_fx3d.py` |
| the seam: do not promise it is invisible | 2.0 or 1.8 mm across, depending on which surface butts; **not measured on our mount** | [10](10-mechanical.md) |
| power: size the supply from the SKU | still open: 5 V, ≥ 8 A wanted. Glasses scenes are thin lines on black | [12](12-bringup.md) phase 0 |
| which filter is on which eye | **open**: the owner, with the glasses on, in `calib` page 3 | the brief §7 |

## Decisions taken

| Date | Decision | Why |
|---|---|---|
| 2026-09-18 | Previews first, rendered by the firmware's own code; the owner picks | the owner's answer |
| 2026-09-18 | Anaglyph is an output mode of the same scenes, not a separate project | the brief wants real 3D coordinates; the hardware is known; the worktree's rules put the work in `src/fx3d/` |
| 2026-09-18 | Two layers: scenes, and looks for any page | the owner, 15:22 and 15:24 |
| 2026-09-18 | A look gets the page's frame by capturing the drawing calls, not by reading the DMA buffer | the buffer is private in the library; every page draws through `display`'s calls |
| 2026-09-18 | `float` only, never `double` | single-precision FPU, `double` emulated; the host test builds the model with `-Wdouble-promotion -Werror` |
| 2026-09-18 | Linear light inside, the CIE encode once at the end | the library applies CIE 1931 to every pixel |
| 2026-09-18 | Off-axis stereo with a zero-parallax plane, no toe-in; disparity limited through the baseline | the brief §4 |
| 2026-09-18 | Red-blue first; red-cyan once green has been checked through the filters | the brief §7 |
| 2026-09-18 | No serial console for fx3d: control through `/api/fx3d` | the IR console reads every byte of `Serial`; two readers would steal from each other |
| 2026-09-18 | The first touch of the knob gives the panel back | a web-forced mode once trapped the knob (HANDOFF 2026-09-14) |

## Step by step

1. **Done:** the model, 14 scenes, their previews and the host test.
2. **Done:** the looks, tried on six real pages on the host.
3. **In code, not yet flashed:** on the panel - the capture, `/api/fx3d`
   (scene or look, mode, depth, swap, gains), the owner's remote at `/fx3d`
   (the calibration's six steps with what to look for, every scene, every
   look), and the bench: in the bench env it starts itself 20 s after boot; in
   any build with the flag, `/api/fx3d?bench=1` or the page's button starts
   it. So one flash of the ordinary firmware with `-DFX3D_ENABLED` serves both
   the measurement and the glasses. The integration session flashes it.
4. The owner with the glasses on the panel (his answer at 15:44: all of them,
   judged with the glasses): `calib` first (which eye is red, red-blue or
   red-cyan, crosstalk), then every scene and look; the choice.
5. The chosen ones in their places: a clock style, ambient, pages on the knob,
   the visualizer's bands into `terrain`; the profile in NVS; a portal card.
6. Later possibilities, laid down: page changes as a turning cube; a look per
   page; `blobs` at half resolution if the bench asks for it; a finer land
   mask for the globe (Natural Earth 1:110m, a download the owner approves).

## Open questions

- **Owner, with the glasses on:** which eye is red; red-blue or red-cyan; how
  much depth is comfortable; which scenes and looks.
- **Panel (integration session):** the blit's cost and every scene's and
  look's frame time - the bench.
- **Where it runs:** in the render tick. A task on core 0 only if a chosen
  scene needs it: its stack would be internal RAM.
- **The page underneath** keeps its own schedule while a scene covers it
  (a board on screen keeps fetching). Start scenes from the clock page until
  scenes are pages of their own.

## Status

| | |
|---|---|
| Checked in code | the model, 14 scenes, 8 looks, the capture, `/api/fx3d`, the page `/fx3d`, the bench; `feat/fx3d` `331b83f` |
| Checked on the host | `tools/fx3d/check_fx3d.py`: 132,405 checks, 0 failed, C++11 and C++17 under ASan/UBSan, and the page's script parses; previews by `render.py --showreel` and `looks.py` |
| Checked by building | flag off unchanged (103,376 B RAM, 2,270,697 B flash; the preprocessed `main.cpp` identical to the branch's); flag on +192 B RAM, +56,616 B flash; the bench env builds; flag matrix 54/54 |
| Checked on the panel | nothing yet |
| Waits for the owner | the glasses (his answer at 15:44: all of them, judged with the glasses) |
